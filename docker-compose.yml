services:
  git-forest:
    build: .
    container_name: git-forest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - forest-data:/app

volumes:
  forest-data: